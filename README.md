# botNico

Repositorio local para el proyecto botNico.

Instrucciones rápidas:

- Crear y activar el entorno virtual:

  Windows PowerShell:

  ```powershell
  python -m venv venv
  .\venv\Scripts\Activate.ps1
  ```

- Instalar paquetes (ya instalados en este entorno):

  ```powershell
  .\venv\Scripts\python.exe -m pip install -r requirements.txt
  ```
